"""
Pod used to interact with autofill objects

Usage examples:
	Enter "46808" into the "Postal Code" autofill
"""

autofill://td{OKRA_TEXT_CLS}/following-sibling::td/input[contains(@onblur, "autoFill")]
	|//input[contains(@onblur, "autoFill") and contains(@onblur, "{OKRA_TEXT}")]
	|//label{OKRA_TEXT_CLS}/parent::*/following-sibling::*//input[contains(@onblur, "autoFill")]
	|//input[contains(@onblur, "autoFill")]|@name|@id



@@@
def blurAutofill():
	from selenium.webdriver.common.keys import Keys
	ele = driver.getElement(xpath='//*[contains(@onblur, "autoFill")]')
	ele.send_keys(Keys.TAB)
@@@



@Enter
"""
def parse(v):
   return (v, )
"""
Wait for page
Type "{0}!" into the autofill
@Wait for AJAX@driver.wcutils.waitForAJAX(timeout=60, quiet=30)
@Blur the autofill@blurAutofill()
@Wait for the page to finish loading@driver.waitFor(driver, lambda d: d.getElement(xpath='//div[@class="wcstatus_bar" and style="opacity: 0;"]'), expected_return=True, timeout=60)

